export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  return (
    <div className="text-[12px] animate-pulse h-screen w-screen flex justify-center items-center">
      {" "}
      Loading...
    </div>
  );
}
